# Aurbot

## Install
> mkdir -p data/build data/repo

## Configure
> Edit commit.sh for REPONAME
> Edit docker-compose for traefik Host
> Add packages in aurbot.conf (along with AUR dependencies of packages)

## Run
> docker-compose up -d

